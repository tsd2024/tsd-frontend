enum ActionType {
    CREATE = "create",
    JOIN = "join",
    REVEAL = "reveal",
    PLAY_CARD = "play_card",
    LOBBY_STATE = "lobby_state",
    CANCEL = "cancel",
    NEXT_ROUND = "next_round",
}

export default ActionType;